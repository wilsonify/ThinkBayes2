post:
  tags: [counts]
  operationId: probability_to_odds
  x-openapi-router-controller: rest_bayes.controllers.math_controller
  description: "posterior of a coin"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "probability-to-odds-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "probability-to-odds-output.yaml"
