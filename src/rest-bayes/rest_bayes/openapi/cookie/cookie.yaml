post:
  tags: [distributions]
  operationId: cookie
  x-openapi-router-controller: rest_bayes.controllers.math_controller
  description: "Using conditional Probability of A given B to compute conditional probability of B given A"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "cookie-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "cookie-output.yaml"
