post:
  tags: [mixture]
  operationId: soccer
  x-openapi-router-controller: rest_bayes.controllers.math_controller
  description: "posterior of a coin"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "soccer-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "soccer-output.yaml"
